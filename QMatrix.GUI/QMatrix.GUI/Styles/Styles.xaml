<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QMatrix.GUI">

    <!-- 布尔值取反转换器 -->
    <local:BoolNegationConverter x:Key="BoolNegationConverter" />
    <local:DateTimeFormatConverter x:Key="DateTimeFormatConverter" />

    <!-- QMatrix 品牌色 -->
    <SolidColorBrush x:Key="QMatrixPrimaryBrush" Color="#0078D4" />
    <SolidColorBrush x:Key="QMatrixSecondaryBrush" Color="#106EBE" />
    
    <!-- WinUI3 风格圆角 -->
    <CornerRadius x:Key="ControlCornerRadius">4</CornerRadius>
    <CornerRadius x:Key="OverlayCornerRadius">8</CornerRadius>
    <CornerRadius x:Key="LargeOverlayCornerRadius">12</CornerRadius>
    
    <!-- 动画效果 -->
    <Storyboard x:Key="MessageEnterAnimation">
        <DoubleAnimation 
            Storyboard.TargetProperty="Opacity"
            From="0" 
            To="1" 
            Duration="0:0:0.3"/>
        <DoubleAnimation 
            Storyboard.TargetProperty="Translation.Y"
            From="20" 
            To="0" 
            Duration="0:0:0.3"/>
    </Storyboard>

    <!-- 消息气泡样式 -->
    <Style x:Key="UserMessageBubbleStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource QMatrixPrimaryBrush}" />
        <Setter Property="CornerRadius" Value="{StaticResource OverlayCornerRadius}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="0,4" />
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <Style x:Key="AgentMessageBubbleStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefault}" />
        <Setter Property="CornerRadius" Value="{StaticResource OverlayCornerRadius}" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="Margin" Value="0,4" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefault}" />
    </Style>

    <!-- Agent 步骤卡样式 -->
    <Style x:Key="AgentStepCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource LayerFillColorDefault}" />
        <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefault}" />
    </Style>

    <!-- 字体设置 -->
    <FontFamily x:Key="DefaultFontFamily">Segoe UI Variable, Segoe UI, sans-serif</FontFamily>
    
    <!-- 标题样式 -->
    <Style x:Key="AppTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
    </Style>

    <!-- 副标题样式 -->
    <Style x:Key="AppSubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Opacity" Value="0.6" />
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
    </Style>
</ResourceDictionary>
